<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipe Manager</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <nav>
            <h1>Recipe Manager</h1>
            <div class="nav-links">
                <a href="#" class="active">Home</a>
                <a href="#" id="savedRecipes">Saved Recipes</a>
                <a href="#" id="createRecipe">Create Recipe</a>
            </div>
        </nav>
    </header>

    <main>
        <section class="search-section">
            <h2>Discover Recipes</h2>
            <div class="search-container">
                <input type="text" id="searchInput" placeholder="Search for recipes...">
                <button id="searchButton">Search</button>
            </div>
        </section>

        <section class="results-section">
            <div id="searchResults" class="recipe-grid"></div>
        </section>

        <section id="createRecipeForm" class="form-section hidden">
            <h2>Create New Recipe</h2>
            <form id="recipeForm">
                <div class="form-group">
                    <label for="title">Recipe Title</label>
                    <input type="text" id="title" name="title" required>
                </div>
                <div class="form-group">
                    <label for="ingredients">Ingredients (one per line)</label>
                    <textarea id="ingredients" name="ingredients" required></textarea>
                </div>
                <div class="form-group">
                    <label for="instructions">Instructions</label>
                    <textarea id="instructions" name="instructions" required></textarea>
                </div>
                <div class="form-row">
                    <div class="form-group">
                        <label for="cookingTime">Cooking Time (minutes)</label>
                        <input type="number" id="cookingTime" name="cookingTime" required>
                    </div>
                    <div class="form-group">
                        <label for="servings">Servings</label>
                        <input type="number" id="servings" name="servings" required>
                    </div>
                </div>
                <button type="submit">Create Recipe</button>
            </form>
        </section>

        <section class="inventory-section">
            <h2>What's in My Fridge?</h2>
            <form id="inventoryForm" class="inventory-form">
                <div class="inventory-input-wrapper">
                    <input type="text" id="inventoryInput" placeholder="Add an ingredient...">
                </div>
                <button type="submit">Add</button>
            </form>
            <div id="inventoryList" class="inventory-chips"></div>
            <button id="clearInventory" class="clear-inventory-btn" type="button">Clear</button>
            <button id="findByInventory" class="find-inventory-btn">Find Recipes With My Ingredients</button>
        </section>
        <section class="results-section">
            <div id="inventoryResults" class="recipe-grid"></div>
        </section>
    </main>

    <script src="/js/main.js"></script>
</body>
</html> 